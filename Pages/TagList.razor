@using MIDIPianoJukebox.Data

@foreach (var tag in Tune.Tags)
{
    <MudChip Text="purple" Color="Color.Success" Size="Size.Small" Href="@("/tag/" + tag)">@tag</MudChip>
}

@code {
    [Parameter] public Tune Tune { get; set; }
}