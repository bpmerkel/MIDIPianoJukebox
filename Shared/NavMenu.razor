@using MIDIPianoJukebox.Data
@inject JukeboxService JukeboxService

<MatNavMenu Multi="true">
    <MatNavItem Href="/"><MatIcon Icon="@MatIconNames.Home"></MatIcon> Home</MatNavItem>
    <MatNavItem Href="playlists"><MatIcon Icon="@MatIconNames.Queue_music"></MatIcon> Playlists</MatNavItem>
    <MatNavItem Href="settings"><MatIcon Icon="@MatIconNames.Settings"></MatIcon> Settings</MatNavItem>
    @*<MatNavSubMenu Expanded="true">
        <MatNavSubMenuHeader>
            <MatNavItem AllowSelection="false">Playlist</MatNavItem>
        </MatNavSubMenuHeader>
        <MatNavSubMenuList>
            @if (jukebox != null)
            {
                foreach (var playlist in jukebox.Playlists.Where(p => p.Tunes.Count > 0).OrderBy(p => p.Name))
                {
                    <MatNavItem Href="@($"/playlist/{playlist.Name}")" Class="navItem">
                        @playlist.Name (@($"{playlist.Tunes.Count:#,##0}"))
                    </MatNavItem>
                }
            }
        </MatNavSubMenuList>
    </MatNavSubMenu>
    <MatNavSubMenu Expanded="true">
        <MatNavSubMenuHeader>
            <MatNavItem AllowSelection="false">Library</MatNavItem>
        </MatNavSubMenuHeader>
        <MatNavSubMenuList>
            @if (jukebox != null)
            {
                <MatNavItem Href="/library/all" Class="navItem">
                    All (@($"{jukebox.Tunes.Count:#,##0}"))
                </MatNavItem>
                foreach (var lib in jukebox.Libraries.OrderBy(g => g.Name))
                {
                    <MatNavItem Href="@($"/library/{lib.Name}")" Class="navItem">
                        @lib.Name (@($"{lib.Tunes.Count:#,##0}"))
                    </MatNavItem>
                }
            }
        </MatNavSubMenuList>
    </MatNavSubMenu>*@
</MatNavMenu>